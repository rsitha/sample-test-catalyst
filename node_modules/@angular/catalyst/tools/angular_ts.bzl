load("//tools:typescript.bzl", "ts_project")

def ng_ts_project(name, tsconfig = "//:tsconfig", srcs = [], angular_assets = [], **kwargs):
    ts_project(
        name = name,
        tsconfig = tsconfig,
        tsc = "@npm//@angular/compiler-cli/bin:ngc",
        srcs = srcs + angular_assets,
        **kwargs
    )
